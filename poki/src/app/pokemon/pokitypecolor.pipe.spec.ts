import { PokitypecolorPipe } from './pokitypecolor.pipe';

describe('PokitypecolorPipe', () => {
  it('create an instance', () => {
    const pipe = new PokitypecolorPipe();
    expect(pipe).toBeTruthy();
  });
});
